#ifndef GRAPH_VIEW_PAGE_TEMPLATE_H
#define GRAPH_VIEW_PAGE_TEMPLATE_H

/// Page that draws a hive's graph structure as a 3D WebGL scene, laying the nodes out itself from the
/// structure a graph view surface map serves it.
extern const char* const graphViewPageTemplate;

#endif
